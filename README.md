# ROMAL frontend

## Development
You can use `npm` instead of `yarn` but it will be slower to install. 

#### Depencies:
    yarn install

#### Watch & run:
    yarn serve

## Minify & Build
    yarn build
